"""Klar - a call intelligence pipeline on the ElevenLabs API.

Klar (German for "clear") turns a call/meeting recording into a structured brief:
summary, action items with owners, decisions, open questions, and per-speaker
sentiment - in the recording's own language.

Public entry points:
    python -m klar run <path> [--customer NAME]
    python -m klar runs
    python -m klar eval
"""

__version__ = "0.1.0"
